# The main pipeline for bronze_pipeline_sql

resources:
  pipelines:
    bronze_pipeline_sql_etl:
      name: bronze_pipeline_sql_etl
      catalog: ${var.catalog}
      schema: ${var.schema}
      serverless: true
      root_path: "."

      libraries:
        - glob:
            include: transformations/**
